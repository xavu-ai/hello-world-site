document.addEventListener('DOMContentLoaded', () => {
    const app = document.getElementById('app');
    app.textContent = 'JavaScript loaded successfully!';
    console.log('Static file serving is working!');
});
